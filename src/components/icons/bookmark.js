import ReactSvg from "react-svg";
import { SVG_ICONS_PATH } from "utils/constants";
const Bookmark = () => (
  <ReactSvg className="svg-icon" src={`${SVG_ICONS_PATH}/bookmark.svg`} />
);

export default Bookmark;
