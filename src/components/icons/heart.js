import ReactSvg from "react-svg";
import { SVG_ICONS_PATH } from "utils/constants";
const Heart = () => (
  <ReactSvg className="svg-icon" src={`${SVG_ICONS_PATH}/heart.svg`} />
);

export default Heart;
