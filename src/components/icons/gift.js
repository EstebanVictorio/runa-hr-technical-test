import ReactSvg from "react-svg";
import { SVG_ICONS_PATH } from "utils/constants";

const Gift = () => (
  <ReactSvg className="svg-icon" src={`${SVG_ICONS_PATH}/gift.svg`} />
);

export default Gift;
