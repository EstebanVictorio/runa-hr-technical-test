import styled from "styled-components";

const StyledGameCard = styled.div`
  width: 280px;
  height: 240px;
  margin: 0 15px;
  border-radius: 4px;
  border: 1px solid skyblue;
`;

const GameCard = () => <StyledGameCard />;

export default GameCard;
